import appDevPost from "./appDevPost-schema";
import frontendPost from "./frontendPost-schema";
import project from "./project-schema";

const schemas = [project, appDevPost, frontendPost];

export default schemas;
